# API Configuration
# Leave empty to use proxy (recommended for development and production with nginx)
# Set to full URL (e.g., http://localhost:8000/api/v1) to bypass proxy
VITE_API_BASE_URL=

# Proxy Target (for Vite dev server)
# Only needed if backend is not on localhost:8000
# This is the base backend URL (without /api/v1)
VITE_PROXY_TARGET=http://localhost:8000

# Feature Flags
VITE_FEATURE_FLAG_SAMPLE=false

